import React from "react";

const NotificationDataContext = React.createContext(0);

export const NotificationDataContextProvider = NotificationDataContext.Provider;

export default NotificationDataContext;
